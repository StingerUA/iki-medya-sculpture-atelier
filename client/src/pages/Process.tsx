import { ArrowDown, ArrowUpRight } from "lucide-react";
import { Link } from "wouter";
const phases = [["01", "Brief", "We begin with the intended setting, visual direction and use case."], ["02", "Define", "Scale, form, finish and technical requirements are resolved for the project."], ["03", "Produce", "The confirmed design proceeds through the studio’s production workflow."], ["04", "Place", "Delivery and final placement are planned around the specific site."]];
export default function Process() { return <main className="page process-page"><section className="page-intro page-intro--wide"><p className="eyebrow">Studio approach</p><h1>From concept<br />to <em>presence.</em></h1><p>Each project is considered in relation to space. The product data and 3D previews in this storefront will be added only after a work has been verified for publication.</p><ArrowDown className="intro-arrow" size={19} /></section><section className="process-list">{phases.map(([number, title, text]) => <article key={number} className="process-row"><span>{number}</span><h2>{title}</h2><p>{text}</p></article>)}</section><section className="process-cta"><p className="eyebrow">Start with context</p><h2>Have a project<br />in mind?</h2><Link href="/contact" className="button button--light">Tell us about it <ArrowUpRight size={17} /></Link></section></main>; }
