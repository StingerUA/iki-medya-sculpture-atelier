import { ArrowUpRight } from "lucide-react";
import { Link } from "wouter";
import { sculptures } from "@shared/catalog";
import SculptureArt from "@/components/SculptureArt";
export default function Collection() { return <main className="page collection-page"><section className="page-intro"><p className="eyebrow">Collection studies</p><h1>Made for<br /><em>the scale</em> of space.</h1><p>Three sculptural studies establish the collection’s visual language. Each is developed as a commissioned piece, with its final scale, material and finish shaped around the setting.</p></section><section className="collection-list">{sculptures.map((sculpture, index) => <Link key={sculpture.id} href={`/sculptures/${sculpture.slug}`} className="collection-card"><div className="collection-card__meta"><span>0{index + 1}</span><span>{sculpture.category}</span><ArrowUpRight size={19} /></div><SculptureArt visual={sculpture.visual} className="collection-card__art" /><div className="collection-card__caption"><h2>{sculpture.title}</h2><p>{sculpture.description}</p></div></Link>)}</section><section className="collection-note"><span>Need a particular scale, finish or form?</span><Link href="/contact" className="text-link">Discuss a commission <ArrowUpRight size={17} /></Link></section></main>; }
